(()=>{'use strict';const c=document.querySelector('#gameCanvas'),ctx=c.getContext('2d');let s,cols,rows,cell,snake,food,dir,next,score,running,timer;function resize(){s=GameShell.fitCanvas(c,1,720);cell=Math.max(14,Math.floor(Math.min(s.width,s.height)/24));cols=Math.floor(s.width/cell);rows=Math.floor(s.height/cell);reset(false)}function reset(play=true){snake=[{x:Math.floor(cols/2),y:Math.floor(rows/2)},{x:Math.floor(cols/2)-1,y:Math.floor(rows/2)}];dir=next={x:1,y:0};score=0;running=play;place();GameShell.setScore(0);clearInterval(timer);if(play)timer=setInterval(step,115)}function place(){do{food={x:Math.floor(Math.random()*cols),y:Math.floor(Math.random()*rows)}}while(snake.some(p=>p.x===food.x&&p.y===food.y))}function turn(x,y){if(x===-dir.x&&y===-dir.y)return;next={x,y}}function step(){dir=next;const h={x:snake[0].x+dir.x,y:snake[0].y+dir.y};if(h.x<0||h.x>=cols||h.y<0||h.y>=rows||snake.some(p=>p.x===h.x&&p.y===h.y)){running=false;clearInterval(timer);GameShell.best('snake',score);GameShell.showOverlay('Rắn đã va chạm',`Điểm ${score} · Kỷ lục ${GameShell.best('snake')}`,'Chơi lại',()=>reset(true));return}snake.unshift(h);if(h.x===food.x&&h.y===food.y){score++;GameShell.setScore(score);place()}else snake.pop()}function draw(){ctx.fillStyle='#07120d';ctx.fillRect(0,0,s.width,s.height);ctx.strokeStyle='rgba(255,255,255,.035)';for(let x=0;x<=cols;x++){ctx.beginPath();ctx.moveTo(x*cell,0);ctx.lineTo(x*cell,rows*cell);ctx.stroke()}for(let y=0;y<=rows;y++){ctx.beginPath();ctx.moveTo(0,y*cell);ctx.lineTo(cols*cell,y*cell);ctx.stroke()}ctx.fillStyle='#ff4f58';ctx.beginPath();ctx.arc((food.x+.5)*cell,(food.y+.5)*cell,cell*.36,0,Math.PI*2);ctx.fill();snake.forEach((p,i)=>{ctx.fillStyle=i?'#50c878':'#9ff0b9';ctx.fillRect(p.x*cell+1,p.y*cell+1,cell-2,cell-2)});requestAnimationFrame(draw)}function key(e){const m={ArrowUp:[0,-1],KeyW:[0,-1],ArrowDown:[0,1],KeyS:[0,1],ArrowLeft:[-1,0],KeyA:[-1,0],ArrowRight:[1,0],KeyD:[1,0]};if(m[e.code]){e.preventDefault();turn(...m[e.code])}}addEventListener('keydown',key);GameShell.swipe(c,d=>({up:()=>turn(0,-1),down:()=>turn(0,1),left:()=>turn(-1,0),right:()=>turn(1,0)}[d]()));addEventListener('resize',resize);resize();GameShell.showOverlay('Rắn săn mồi','PC: phím mũi tên/WASD · Mobile: vuốt','Bắt đầu',()=>reset(true));draw()})();
