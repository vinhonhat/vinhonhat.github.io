(()=>{'use strict';const root=document.querySelector('[data-game-root]');root.className='game-panel';root.innerHTML='<div class="game-stat-row"><div class="game-stat"><strong id="pairs">0/8</strong><small>Cặp đúng</small></div><button class="game-action" id="restart">Trộn lại</button></div><div id="memory" class="memory-grid"></div><div class="game-overlay" data-game-overlay><div class="game-dialog"><h1 data-overlay-title>Nối hình</h1><p data-overlay-message>Tìm các cặp biểu tượng giống nhau.</p><button class="game-primary" data-overlay-action>Bắt đầu</button></div></div>';const st=document.createElement('style');st.textContent='.memory-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:9px}.memory-card{aspect-ratio:1;border:0;border-radius:13px;background:#273246;color:transparent;font-size:clamp(26px,7vw,48px);font-weight:900;cursor:pointer;transform-style:preserve-3d;transition:.18s}.memory-card.open,.memory-card.done{background:#fff4df;color:#222;transform:rotateY(180deg)}.memory-card.done{background:#d9f7e4;opacity:.7}';document.head.append(st);const el=document.querySelector('#memory'),pairs=document.querySelector('#pairs');const icons=['🍎','🚗','🐱','🌙','🎵','⚽','🌸','⭐'];let cards,first,busy,matched,moves;function reset(){cards=[...icons,...icons].sort(()=>Math.random()-.5).map((v,i)=>({v,i,open:false,done:false}));first=null;busy=false;matched=0;moves=0;render()}function render(){el.innerHTML=cards.map((c,i)=>`<button class="memory-card${c.open?' open':''}${c.done?' done':''}" data-i="${i}">${c.v}</button>`).join('');pairs.textContent=`${matched}/8`;GameShell.setScore(moves,'Lượt')}function pick(i){if(busy||cards[i].open||cards[i].done)return;cards[i].open=true;if(first===null){first=i;render();return}moves++;const j=first;first=null;if(cards[j].v===cards[i].v){cards[j].done=cards[i].done=true;matched++;render();if(matched===8){GameShell.best('memory',Math.max(1,100-moves));setTimeout(()=>GameShell.showOverlay('Hoàn thành!',`Anh ghép xong trong ${moves} lượt.`,'Chơi lại',reset),250)}}else{busy=true;render();setTimeout(()=>{cards[j].open=cards[i].open=false;busy=false;render()},650)}}el.onclick=e=>{const b=e.target.closest('[data-i]');if(b)pick(Number(b.dataset.i))};document.querySelector('#restart').onclick=reset;reset();GameShell.showOverlay('Nối hình','Nhấn hai ô để tìm cặp giống nhau.','Bắt đầu',reset)})();
